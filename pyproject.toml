[project]
name = "nuance"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bittensor>=9.1.0",
    "pydantic-settings>=2.8.1",
]
